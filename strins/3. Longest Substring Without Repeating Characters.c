// Given a string s, 
// find the length of the longest substring
// without repeating characters.

#include <stdio.h>

int main() {
    for (int i = 0; i < 10; i++)
    {
        printf("%d \n", i);
    }
    
    printf("Hola, Mundo!\n");
    return 0;
}
